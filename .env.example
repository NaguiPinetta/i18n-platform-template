# Supabase Configuration (REQUIRED for auth/workspaces)
# Get these from: Supabase Dashboard → Settings → API
PUBLIC_SUPABASE_URL=your_supabase_url
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Application Configuration
PUBLIC_DEFAULT_LOCALE=en
PUBLIC_SUPPORTED_LOCALES=en,es,fr,ar

# Development (Optional)
# Prefill email on login page in development mode only
VITE_DEV_EMAIL=

# Translation Service (Optional)
# TRANSLATION_SERVICE_API_KEY=your_api_key

# Security Note:
# - PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY are safe to expose (public variables)
# - The anon key is designed to be public; security is provided by Row Level Security (RLS)
# - NEVER commit your actual .env file to version control
# - NEVER include service role keys or other secrets in this file
